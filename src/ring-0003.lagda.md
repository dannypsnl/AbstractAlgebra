```agda
module ring-0003 where

open import MLTT.Spartan

open import ring-0000
open Ring {{...}}
open import ring-0002
```

負負得正通常可能在說兩件事

1. $-a \times -b = a \times b$
2. $--a = a$

關於第二個，可以看到因為 $-a$ 的反元素是 $a$ 也可以是 $--a$，因此 $--a$ 一定等於 $a$（因為 Ring 的加法部分是一個 commutative group）。
第一個則比較複雜一些，證明如下

```
proposition-2 : {R : 𝓤 ̇ } {{_ : Ring R}} → (a b : R) → - a ⊗ - b ＝ a ⊗ b
proposition-2 {_}{R} a b =
  (- a ⊗ - b) ＝⟨ lemma a (- b) ⟩
  - (a ⊗ - b) ＝⟨ ap -_ (lemma2 a b) ⟩
  - - (a ⊗ b) ＝⟨ lemma3 (a ⊗ b) ⟩
  a ⊗ b ∎
  where
  lemma : (a b : R) → - a ⊗ b ＝ - (a ⊗ b)
  lemma a b = ring-cancel-right (II ∙ I ⁻¹)
    where
    I : (- (a ⊗ b)) ⊕ a ⊗ b ＝ 𝟘R
    I = (- (a ⊗ b)) ⊕ a ⊗ b ＝⟨ cancel .pr₁ ⟩
        𝟘R ∎
    II : (- a ⊗ b) ⊕ a ⊗ b ＝ 𝟘R
    II = (- a ⊗ b) ⊕ a ⊗ b ＝⟨ distrib1 ⁻¹ ⟩
        (- a ⊕ a) ⊗ b      ＝⟨ ap (_⊗ b) (cancel .pr₁) ⟩
        𝟘R ⊗ b             ＝⟨ lemma-1 b .pr₂ ⟩
        𝟘R ∎
  lemma2 : (a b : R) → a ⊗ - b ＝ - (a ⊗ b)
  lemma2 a b = ring-cancel-right (II ∙ I ⁻¹)
    where
    I : (- (a ⊗ b)) ⊕ a ⊗ b ＝ 𝟘R
    I = cancel .pr₁
    II : (a ⊗ - b) ⊕ a ⊗ b ＝ 𝟘R
    II = (a ⊗ - b) ⊕ a ⊗ b ＝⟨ distrib2 ⁻¹ ⟩
        a ⊗ (- b ⊕ b)      ＝⟨ ap (a ⊗_) (cancel .pr₁) ⟩
        a ⊗ 𝟘R             ＝⟨ lemma-1 a .pr₁ ⟩
        𝟘R ∎
  lemma3 : (x : R) → - - x ＝ x
  lemma3 x = ring-cancel-right (I ∙ II ⁻¹)
    where
    I : - - x ⊕ - x ＝ 𝟘R
    I = cancel .pr₁
    II : x ⊕ - x ＝ 𝟘R
    II = cancel .pr₂
```
